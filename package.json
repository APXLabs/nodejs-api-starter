{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "awilix": "^3.0.8",
    "awilix-koa": "^2.1.1",
    "fejl": "^1.0.2",
    "keyblade": "^0.3.2",
    "koa": "^2.5.1",
    "koa-bodyparser": "^4.2.1",
    "koa-compress": "^3.0.0",
    "koa-respond": "^1.0.1",
    "koa-router": "^7.4.0",
    "mongodb": "^3.1.3",
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "axios": "^0.18.0",
    "eslint": "^4.13.1",
    "eslint-config-prettier": "^2.9.0",
    "eslint-config-standard": "^11.0.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-jest": "^21.21.0",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-prettier": "^2.6.2",
    "eslint-plugin-promise": "^3.8.0",
    "eslint-plugin-standard": "^3.1.0",
    "jest": "^23.4.0",
    "lodash": "^4.17.10",
    "nodemon": "^1.18.3",
    "prettier": "^1.9.2",
    "smid": "^0.1.1"
  },
  "scripts": {
    "lint": "eslint --ignore-path .gitignore --ignore-pattern \"!**/.*\" .",
    "fix": "eslint --ignore-path .gitignore --ignore-pattern \"!**/.*\" --fix .",
    "test": "env $(cat .env) jest",
    "build": "node tools/build.js",
    "build-watch": "node tools/build.js --watch",
    "docker": "docker-compose run --rm api yarn",
    "docker-install": "docker-compose run --rm --no-deps api yarn install",
    "docker-upgrade": "docker-compose run --rm --no-deps api yarn upgrade",
    "docker-test": "docker-compose run api yarn install && yarn test",
    "docker-test-watch": "docker-compose run api yarn install && yarn test --watch",
    "dev": "env $(cat .env) node src/server.js",
    "start": "node build/server.js"
  }
}
